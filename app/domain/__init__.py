"""Domain layer — pure Python, no I/O, no framework imports.

Contains entities, validation, the in-process event bus, cancellation
primitives, and the interfaces (Protocols) that infrastructure implements.
"""
