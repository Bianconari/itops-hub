"""Centralized theme system: design tokens -> one application stylesheet.

Light and dark palettes live here and nowhere else; PyQtGraph colors (from
v0.4) are derived from the same tokens so charts always match the UI.
"""
