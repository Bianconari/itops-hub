"""Local FastAPI service — ships in v1.5 (milestone M10).

Planned: localhost-only binding, per-session token auth, OpenAPI docs at
/docs, endpoints per docs/api.md, sharing the exact same service layer as
the desktop UI. This package intentionally contains no code until then.
"""
