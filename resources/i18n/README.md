# i18n resources

The Qt Linguist pipeline (`tr()` + `.ts`/`.qm` files) is wired into the build
from v0.3. v1.5 ships English only (owner decision Q3); additional languages
are translation-only additions via `pyside6-lupdate` / `pyside6-lrelease`.
